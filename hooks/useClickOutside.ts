// motion-primitives components import from "@/hooks/useClickOutside"; the CLI placed the
// implementation next to the components, so re-export it from the expected path.
export { default } from "@/components/motion-primitives/useClickOutside";
